# Performance Notes

Benchmarks use warmed runtimes and fake/simple adapters where possible. Results are Unity Editor orchestration measurements, not mobile-device proof.

Recorded benchmark dimensions:

- Unity version
- operation count
- tick count
- elapsed time
- allocation bytes
- enemy count
- mount count
- direct/projectile mix
- benchmark path
